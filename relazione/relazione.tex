\documentclass[12pt, a4paper, twoside]{article}

% Import
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{graphicx}
\usepackage[utf8]{inputenc}
\usepackage{pifont}
\usepackage[italian]{babel}
\usepackage{tocloft}
\usepackage{geometry}
\usepackage{times}
\usepackage{setspace}
\usepackage{indentfirst}
\usepackage{ragged2e}
\usepackage{amsmath}
\usepackage{url}
\usepackage{float}
\usepackage{fancyhdr}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{caption}
\usepackage{hyperref}
\usepackage{cleveref}
\usepackage{chngcntr}

% Impostazioni
\raggedbottom
\numberwithin{table}{section}
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}
\geometry{a4paper, left=2.5cm, right=2.5cm, top=2.5cm, bottom=2.5cm}
\setstretch{1.0}
\setlength{\parindent}{0.5cm}
\justifying
\setlength{\parindent}{0pt}
\renewcommand{\sectionmark}[1]{\markboth{#1}{}}
\renewcommand{\cftbeforesecskip}{0pt} % Imposta lo spazio a 0 punti
\counterwithin{figure}{section}

% CleverRef settings
\Crefname{figure}{Figura}{Figure}
\Crefname{table}{Tabella}{Tabelle}
\Crefname{section}{Sezione}{Sezioni}
\Crefname{equation}{Equazione}{Equazioni}

% Configurazione opzionale dello stile dei listings
\lstset{
    frame=tb, % Linea sopra e sotto il blocco di codice
    language=Python, % Specifica il linguaggio di default
    aboveskip=3mm,
    belowskip=3mm,
    showstringspaces=false,
    columns=flexible,
    basicstyle={\small\ttfamily},
    numbers=none,
    numberstyle=\tiny\color{gray},
    keywordstyle=\color{blue},
    commentstyle=\color{green},
    stringstyle=\color{mauve},
    breaklines=true,
    breakatwhitespace=true,
    tabsize=3
}
\captionsetup[lstlisting]{labelformat=empty}

\begin{document}

    % Copertina
    \begin{titlepage}
        \centering
        
        \includegraphics[width=0.2\linewidth]{logounica.png}
        
        \vspace{0.8cm}
        \textbf{UNIVERSITA' DEGLI STUDI DI CAGLIARI} \\
        \vspace{0.8cm}
        \large \textbf{FACOLTÀ DI SCIENZE} \\
        \vspace{0.8cm}
        \large Corso di Laurea Magistrale in Informatica \\
        
        \vspace{4.2cm}
        \LARGE \textbf{Data Mining: Report Finale} \\
        
        \vspace{1.0cm}
        \large \textbf{Gruppo G3.3}
        \vspace{4.0cm}
        
        \begin{flushleft}
            \large \textbf{Componenti:} \\
            \large Aresu Matteo \hfill 60/99/00014 \\
            \large Mantega Gabriele \hfill 60/99/00006 \\
            \large Masala Gabriele \hfill 60/99/00004 \\
        \end{flushleft}
        
        \vspace{3.0cm}
        \large ANNO ACCADEMICO 2025-2026
    \end{titlepage}

    % Indice
    \pagenumbering{arabic}
    \setcounter{page}{2}
    \renewcommand{\contentsname}{Indice}
    \tableofcontents
    \pagebreak

    % Inizio Documento
    \thispagestyle{plain}
    \section{Introduzione}
    Questo report affronta il problema della classificazione dei documenti testuali, applicato all'analisi
    delle interazioni interne dei newsgroup, spazi online storicamente strutturati su infrastrutture decentralizzate, 
    e dedicati al dibattito su topic ben determinati. L'obiettivo centrale del progetto è l'utilizzo di diversi algoritmi
    di classificazione, spaziando dai modelli storici tradizionali (shallow learning) ai modelli a combinazione (ensemble learning),
    al fine di predire il topic di un post partendo dal contenuto dei suoi messaggi.
    Lo studio non si è limitato all'analisi delle prestazioni dei classificatori, ma si estende alla comparazione dei metodi di rappresentazione
    geometrica dei dati in input. a questo scopo sono stati utilizzati due metodi di vettorizzazione: 
    \textbf{TF-IDF} (Term Frequency-Inverse Document Frequency) e \textbf{GloVe} (Global Vectors for Word Representation). 
    
    \vspace{5mm}
    Il presente report è organizzato nei seguenti passi:
    \begin{itemize}
        \item \textbf{\Cref{sec:dataset}} - \textbf{Dataset} Analisi approfondita del dataset 20newsgroups, della sua struttura e delle sue categorie tematiche
        con approfondimento su quelle scelte.

        \item \textbf{\Cref{sec:preprocessing}} - \textbf{Preprocessing} Descrizione del flusso di elaborazione e trasformazione dei dati, differenziando i due metodi di vettorizzazione scelti,
        con relativo approfondimento sulle scelte di implementazione prese durante lo sviluppo del progetto.
        
        \item \textbf{\Cref{sec:application-of-models}} - \textbf{Applicazione modelli} Elenco dei classificatori utilizzati, con i relativi risultati di training e testing sul dataset
        e scelte implementative effettuate sugli iperparametri.
        
        \item \textbf{\Cref{sec:discussion}} - \textbf{Discussione} Argomentazione esaustiva sui risultati ottenuti con considerazioni argomentate e rappresentazione visiva dei dati, mediante tabelle e grafici informativi.

    \end{itemize}

    \pagebreak
    \thispagestyle{plain}
    \section{Dataset}
    \label{sec:dataset}
    Il dataset di riferimento utilizzato per la sperimentazione è \textbf{20newsgroups}, una corpus di circa 18.000 pubblicazioni
    ampiamente utilizzata nel Natural Language Processing (NLP), e distribuite all'interno di venti differenti categorie tematiche. 
    Una caratteristica di questo dataset è l'organizzazione in forma gerarchica dei suoi topic, formalizzata dalla seguente struttura:
    \texttt{macrocategoria.sottocategoria} (con la possibilità di inserire ulteriori sottocategorie, successivamente alla prima).
    La documentazione relativa e i dati di partenza sono consultabili liberamente al seguente link:
    \href{https://www.kaggle.com/datasets/crawford/20-newsgroups}{https://www.kaggle.com/datasets/crawford/20-newsgroups}.

\begin{figure}[ht]
    \begin{lstlisting}[language={}]
From: baalke@kelvin.jpl.nasa.gov (Ron Baalke)
Subject: Galileo Update - 04/15/93
Organization: Jet Propulsion Laboratory
Lines: 113
Distribution: world
NNTP-Posting-Host: kelvin.jpl.nasa.gov
Keywords: Galileo, JPL
News-Software: VAX/VMS VNEWS 1.41    

Forwarded from Neal Ausman, Galileo Mission Director

                                GALILEO
                     MISSION DIRECTOR STATUS REPORT
                             POST-LAUNCH
                         April 9 - 15, 1993

SPACECRAFT

1.  On April 9, real-time commands were sent, as planned, to reacquire
celestial reference   completion of the Low Gain Antenna (LGA-2)
swing/Dual Drive Actuator (DDA) hammer activities.
...
    \end{lstlisting}
    \caption{Estratto di un post del topic \texttt{sci.space}}
    \label{fig:post-example}
\end{figure}

    Ai fini dell'eleborazione del dataset, è stato utilizzato solo un sottoinsieme di cinque categorie per analizzare il comportamento dei classificatori, di cui
    alcune rappresentanti tematiche potenzialmente sovrapponibili concenttualmente. La totalità delle categorie utilizzate in questo scopo sono riportate in \Cref{tab:newsgroups}.
    Dal punto di vista strutturale, ogni singolo documento del dataset corrisponde a un post creato da un utente. In linea con gli standard Usenet, ogni post è strutturato
    nel seguente modo:

    \begin{itemize}
        \item \textbf{Header}: Una sezione iniziale del documento, dove vengono inseriti alcuni metadati tecnici, come l'indirizzo email del mittente, il
        server di provenienza e l'oggetto del messaggio.
        \item \textbf{Body} Il testo effettivo del post scritto dall'utente, un insieme di frasi in linguaggio naturale costituenti il vero nucleo informativo.
        \item \textbf{Footer} Una sezione finale dedicata alle firme automatiche o citazioni personali
    \end{itemize}
    Un post di esempio, che rispetta questa struttura, è osservabile in \ref{fig:post-example}.

    \begin{table}[htbp]
        \centering
        \begin{tabularx}{\textwidth}{lX}
            \toprule
            \textbf{Gruppo di Discussione} & \textbf{Descrizione} \\
            \midrule
            \texttt{rec.autos} & Discussioni su motori, recensioni, manutenzione e novità automobilistiche. \\
            \texttt{rec.sport.baseball} & Spazio per appassionati di baseball: analisi, statistiche e campionati. \\
            \texttt{sci.electronics} & Forum su progettazione di circuiti, componenti e teoria elettronica. \\
            \texttt{sci.med} & Discussioni su medicina, salute, ricerche scientifiche e trattamenti. \\
            \texttt{sci.space} & Spazio dedicato all'esplorazione spaziale, astronomia e tecnologia dei razzi. \\
            \bottomrule
        \end{tabularx}
        \caption{Elenco dei newsgroup utilizzati nel progetto e relative descrizioni}
        \label{tab:newsgroups}
    \end{table}
    
    Il progetto è stato realizzato con l'ausilio dell'infrastruttura di Jupyter, il linguaggio di programmazione Python 
    e delle sue librerie fondamentali, come pandas, scikit-learn, NumPy, Matplotlib. Il dataset è stato caricato in 
    locale tramite la funzione \texttt{datasets.fetch\_20newsgroups} di scikit-learn.
    Il dataset è stato successivamente ripartito in \textbf{training set} e \textbf{test set}, come rappresentato 
    in \ref{fig:distribuzione-set}.
    
    \begin{figure}[ht]
        \centering
        \includegraphics[width=0.6\linewidth]{distribuzione_set.png}
        \caption{Distribuzione degli oggetti tra training set e test set}
        \label{fig:distribuzione-set}
    \end{figure}

    In \ref{fig:distribuzione-classi} viene rappresentata la equa distribuzione delle classi all'interno del training 
    e test set, tenendo presente che la classi rappresentano i gruppi di discussione.
    \begin{figure}[ht]
        \centering
        \includegraphics[width=1\linewidth]{distribuzione_classi.png}
        \caption{Distribuzione delle classi nel dataset in esame}
        \label{fig:distribuzione-classi}
    \end{figure}
    
    \clearpage
    \thispagestyle{plain}
    \section{Preprocessing}
    \label{sec:preprocessing}
    La fase di preprocessing è finalizzata sulla conversione dei dati testuali grezzi, contenuti nei documenti del dataset,
    in rappresentazioni numeriche strutturate, tramite un processo chiamato vettorizzazione. In questo progetto sono stati
    implementati e confrontati due approcci metodologici: la rappresentazione statico-frequenziale \textbf{TF-IDF} e 
    l'utilizzo di vettori semantici densi tramite \textbf{embeddings GloVe}. Entrambe le metodologie sono state configurate
    per lavorare su uno spazio lineare a cento dimensioni, come da specifiche.
    
    \begin{itemize}
        
        \item \textbf{Vettorizzazione TF-IDF} \\
        Il metodo TF-IDF (Term Frequency-Inverse Document Frequency) costruisce un dizionario interno al modello,
        inserendo al suo interno ogni parola presente nel dataset. Per ogni parola presente nel dizionario,
        viene calcolato il suo punteggio IDF. Il nucleo dell'algoritmo prevede il calcolo della frequenza di una parola
        all'interno del singolo post (frequenza \mbox{intra-documento}) pesata inversamente rispetto alla sua diffusione nell'
        intero dataset (frequenza \mbox{inter-documento}). L'intuzione è quella di restituire un valore TF-IDF più alto 
        alle parole rare, in quanto in possesso di elevato potere discriminante l'identificazione del topic di 
        appartenenza dell'intero messaggio. In \Cref{eq:tf-idf} è riportata la formula utilizzata da \texttt{TfidfVectorizer()}, implementato da \texttt{Scikit-learn}, per il calcolo del punteggio TF-IDF per un dato termine $t$ all'interno di un documento $d$. Si noti che i parametri di \textit{default} prevedono lo \textit{smoothing} aggiungendo $1$ per evitare la divisione per zero.

        \begin{equation}
        \begin{split}
            &\text{TF-IDF} = \text{tf}(t,d) \times \text{idf}(t) \\ \\
            &\text{idf}(t) = \log\left(\frac{1+N}{1+\text{df}(t)}\right)+1
        \end{split}
        \label{eq:tf-idf}
        \end{equation}

        \item \textbf{Embedding Semantici GloVe} \\
        A differenza dell'approccio probabilistico e lessicale di TF-IDF, GloVe si appoggia ad un dizionario 
        di vettori pre-addestrato. Ogni parola viene proiettata su uno spazio geometrico continuo a $N$ dimensioni,
        in cui le coordinate precise di un termine sono in grado di rappresentare il suo significato semantico. 
        Questa rappresentazione ha il vantaggio che, parole con significato semantico simile, risulteranno vicine geometricamente
        tramite distanza euclidea. Ciò permette ai classificatori di intercettare relazioni tra parole sinonime e contesti
        logici che il semplice calcolo delle frequenze non sarebbe in grado di scoprire.

    \end{itemize}

    In entrambi i casi, prima della vettorizzazione, avviene un passo di normalizzazione del testo e di pulizia dalle \textit{stop-words}. Queste sono tutti i termini di un linguaggio che non trasmettono un significato semantico particolare ma che fanno parte della grammatica. Un esempio di tali parole è dato da: "is", "the" e "of" nella lingua inglese.
    
    Nel caso di TF-IDF, questa fase di pulizia avviene internamente alla funzione \texttt{TfidfVectorizer()} impostando il parametro \texttt{stop\_words="english"}.
    Nel caso di GloVe, abbiamo implementato le seguenti funzioni:
    \begin{itemize}
        \item \texttt{preprocess\_text(text)} : esegue il \textit{lowecasing}, elimina caratteri speciali e punteggiatura, rimuove le stop-words (ovvero le occorrenza di \texttt{sktext.ENGLISH\_STOP\_WORDS} all'interno del documento) ed effettua la \textit{tokenization} delle parole; 
        \item \texttt{load\_embeddings(file\_path)} : carica il dizionario degli embeddings a partire dal file \texttt{glove.6B.100d.txt}, che presenta al suo interno un vettore di 100 valori numerici per ciascuna parola;
        \item \texttt{get\_document\_embedding(tokens, glove\_embeddings)} : per ciascun token di un documento, ottiene l'embedding corrispondente, se presente nel dizionario GloVe, e calcola la media di tutti questi embedding per ottenere un unico vettore che rappresenta il documento. Se nessun token è presente all'interno del dizionario, il document embedding corrispondente è il vettore nullo.  
    \end{itemize}

    % Bisogna parlare anche delle stopwords, in quanto ricorrente per entrambe le rappresentazioni vettoriali

    % Una volta che sono stati introdotti i due metodi di vettorizzazione, 
    % è necessario parlare dei processi di elaborazione e trasformazione
    % effettivamente svolti all'interno del dataset. Inoltre, le specifiche
    % parlano di grafici e relativa documentazione che noi in questo momento non abbiamo

    % TfIDF: togliere stopwords, impostare max 100 dimensioni e scalare il training set e test set

    % GloVe: apertura dizionario (embeddings), preprocessing (minuscolo, tolta punteggiatura, tokenizzato, rimozione stopword)
    

    \clearpage
    \thispagestyle{plain}
    \section{Applicazione Modelli}
    \label{sec:application-of-models}
    
    \thispagestyle{plain}
    \section{Discussione}
    \label{sec:discussion}
    
    \thispagestyle{plain}
    \section{Conclusioni}

\end{document}